---
title: "biggest_problem"
output: html_document
---

```{r}

#library(plyr)

#library(dplyr)
# General-purpose data wrangling
library(tidyverse)  

# Parsing of HTML/XML files  
library(rvest)    

# String manipulation
library(stringr)   

# Verbose regular expressions
library(rebus)     

# Eases DateTime manipulation
library(lubridate)
```
```{r}
get_link <- function(html,
                     href_text='Transcript'){
  html %>% 
  html_nodes("a") %>% 
  grep(pattern = href_text, value=T) %>%
  read_html() %>%
  html_nodes("a") %>%
  html_attr('href')
}

url_to_html <- function(url){
  url %>% 
    read_html() %>%
    html_text() %>% 
    # Trim additional white space
    str_trim() %>%                       
    # Convert the list into a vector
    unlist()   
}

get_speakers <- function(text){
  text %>% 
  str_match_all("\"?[:alpha:]+:") %>% 
  unlist() %>% 
  unique() %>%
  str_subset("^[:upper:]")
}

text_to_df <- function(text,
                       pattern){
 transcript <- str_split(text, 
                         pattern) %>% 
   unlist()
  transcript <- transcript[-1]
  list_transcript <- regmatches(transcript, regexpr(":", transcript), invert = TRUE)
  df_text <- do.call(rbind.data.frame, list_transcript)
  names(df_text) <- c("speaker", "text")
  return(df_text)
}


### Generate urls
url_episode_base <- 'http://biggest.thedickshow.com/episode-'
url_suffix <- paste0(seq(1:104) %>% as.character(), '/')
vec_url_episodes <- paste0(url_episode_base, url_suffix)
list_episodes <- list(url_episode = vec_url_episodes, episode = seq(1:104))

get_transcript <- function(url_episode, 
                           episode){
  #print(paste0(url_episode, episode, sep =" "))
  # url_episode <- 'http://biggest.thedickshow.com/episode-100/'
  # episode <- 100

  html_episode <- read_html(url_episode)
  url_transcript <- get_link(html_episode)
  text_episode <- url_to_html(url_transcript)
  
  text_episode_stripped <- text_episode %>% 
    str_replace_all("[\r\n]" , " ") %>%
    gsub(pattern = '\"', replacement = "")
  
  speakers <- get_speakers(text_episode_stripped)
  pattern_speakers <- paste0(speakers, collapse="|")
  pattern_dialogue <- paste0("\\s+(?=(", pattern_speakers, "))")
  
  df_text <- text_to_df(text_episode_stripped, pattern_dialogue)
  df_text <- df_text %>% 
    mutate(speaker = as.character(speaker),
           text = trimws(as.character(text)),
           episode = as.character(episode)) %>%
    rownames_to_column('order')
  return(df_text)
}
url_episode
df_test <- get_transcript('http://biggest.thedickshow.com/episode-99/', 99)
write.csv(df_test, 'ep_99.csv', fileEncoding = 'UTF-8', row.names = F)
df_test %>% data.frame()
df_test <- pmap(list_episodes, test)
data.frame(df_test)
df <- as.data.frame(df_test)
df
### Iterate over episodes
# url_episode <- 
html_episode <- read_html(url_episode)
url_transcript <- get_link(html_episode)
text_episode <- url_to_html(url)

text_episode_stripped <- text_episode %>% 
  str_replace_all("[\r\n]" , "") %>%
  gsub(pattern = '\"', replacement = "")

speakers <- get_speakers(text_episode_stripped)
pattern_speakers <- paste0(speakers, collapse="|")
pattern_dialogue <- paste0("\\s+(?=(", pattern_speakers, "))")

df_text <- text_to_df(text_episode_stripped, pattern_dialogue)
df_text <- df_text %>% 
  mutate(speaker = as.character(speaker),
         text = trimws(as.character(text)),
         episode = as.character(episode)) %>%
  rownames_to_column('order')
df_text
# transcript <- 
#   str_split(text_episode_stripped, 
#           pattern_dialogue) %>% 
#   unlist()
# transcript <- transcript[-1]
# list_transcript <- regmatches(transcript, regexpr(":", transcript), invert = TRUE)
# df_text <- do.call(rbind.data.frame, list_transcript)

  # str_match("<a href=\"(.*?)\"") %>% 
  # as.vector() %>% 
  # nth(2)
  #str_trim() %>% 
  # str_detect('Transcript')
  # html_attr('href') # get links
#html_episode %>% trim_text()
```

```{r}





#str_split(transcript, "^[:alpha:]+:", n=1)
df_text <- do.call(rbind.data.frame, list_transcript)

names(df_text) <- c("speaker", "text")

df_text <- df_text %>% mutate(speaker = as.character(speaker),
                   text = trimws(as.character(text)))
df_text %>% head()

write.csv(df_text, "transcript.csv")
#data.frame(Reduce(rbind, list_transcript))


#ldply (list_transcript, data.frame)
```


